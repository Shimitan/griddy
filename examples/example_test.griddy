# example_test.griddy

board (3, 3);

piece p_x
    name: "X"
    limit: 3
    StartPosition: (3, 1)
end

piece p_o
    name: "O"
    limit: 3
    StartPosition: (1, 1)
end

user_input = 0;

fun win_cond ()
    cond = false;
    output "End of game!";
return 0

fun ask_for_number ()
    if @turn_count == 0
        output "Please input a number between 50 and 100:";
    end

    input user_input;

    if user_input > 100
        output "That number is too big!";
        output "Try again!";

    else if user_input < 50
        output "That number is too small!";
        output "Try again!";

    else
        output "Excellent choice!";
        output user_input;

    end
return "Hello"

GAME (false) #( (50 > user_input) || (user_input > 100) )
    output @board;

    p_one = tile (3, 3);
    p_two = tile (3, 1);

    expr = 1 + 2;

    output p_one == p_two;

    #output ask_for_number();
